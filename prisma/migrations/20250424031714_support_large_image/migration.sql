BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[prize_list] ALTER COLUMN [image] NVARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
